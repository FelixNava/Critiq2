# Critiq Full Auto — Runtime State
# Edit STOP=true from any device to halt at the next cron firing.
# Edit URL: https://github.com/FelixNava/Critiq2/edit/review-for-main/.critiq-full-auto-state
# This file is read by /critiq-full-auto skill at session start.
# Format: KEY=VALUE, one per line, lines starting with # are comments.

# --- KILL SWITCH (the only field you should ever edit manually) ---
STOP=false

# --- BUILD METADATA ---
BUILD_START_ET=2026-06-05T02:25:00-04:00
BUILD_LABEL=critiq-beta-foundation-live-supervised
BUILD_TOTAL_TOKEN_BUDGET=4000000
BUILD_PHASE_TOKEN_BUDGET=800000

# --- PHASE TRACKING ---
PHASE_CURRENT=
PHASE_CURRENT_STATUS=
PHASE_CURRENT_PR=
# 2026-06-19 (critiq-fullauto-34-35 firing #2): Phase 34d WIRE recording -> coaching
# DONE -> merged review-for-main (PR #34, squash df96913). NO schema change (every
# linkage column pre-existed). Pre-warm pipeline (src/lib/recording/process.ts:
# processRecordingForScore chains idempotent transcription->scoring, never throws)
# fired post-response (after()) on assign of a COMPLETED recording to a non-null
# account (assign-account route) so the score is ready by debrief time; transcribe/
# score crons stay the net. Debrief route accepts optional recordingId validated by
# the access boundary (rep owns AND assigned to THIS account; invalid->400).
# listAssignedRecordingsForAccount + optional "Was this call recorded?" picker in
# DebriefPanel (renders only when the account has recordings; selection persists).
# Shared inbox formatters de-duped into recordingUi. Value path CLOSED: coaching
# reads the score for debrief.recordingId + snapshots it (Phase 21 path; Phase 26
# guard intact). typecheck+build clean; verify-phase20/21/25/26 no-regression +
# verify-phase34d 27/27; high-effort code-review (3 fixes in-branch); /design-critique
# (1 P1 fixed). DEC-046..048. Phase 34 now FULLY DONE (34a/b/c + 34d). NEXT = Phase 35.
# FLAGGED (UI_VERIFY_PENDING=34d): live UI drive + on-device capture->assign->score->
# coach pass; AI quality of recording-driven coaching (Felix + expert coach).
# 2026-06-19 (critiq-fullauto-34-35 firing #1): Phase 34 ADDITIVE parts 34a/b/c
# DONE -> merged review-for-main (PR #33, squash 0e55638). Quick-record (/record +
# RecordingPanel reusing useRecorder) + recordings inbox (/recordings,
# listRecordingsForUser) + manual assign-to-account (POST /api/recording/[id]/
# assign-account, double-guarded) + nullable recordings.title (mig 0017) + shared
# recordingUi helpers + mobile-first input/touch classes. typecheck+build clean;
# verify-phase 12/13/14/15/16/20/21/25/26 all green (no regression); throwaway
# real-PG probe 16/16; new routes 307->/login. code-review P0/P1:0; /design-critique
# P0+P1 addressed in-branch. DEC-042..045. FLAGGED for Felix: live authenticated UI
# eyeball + audio-capture device test (not driven this autonomous run; SSO-walled
# preview + no connected browser). Per DEC-042 (additive-first build discipline),
# 34d (WIRE recording->coaching: transcription+scoring on assign, then set
# call_debriefs.recordingId so Phase 21 coaching consumes the score; keep the
# un-recorded path + Phase 26 guard intact) is DEFERRED to firing #2.
# Phase 26 DONE 2026-06-18 (aggressive-auto, relaxed gate + high-effort code review). Hallucination
# Guard — the conservative pre-output validator (the locked credibility guardrail). src/lib/
# hallucinationguard/ (types/policy/detect/prompt/anthropic/guard/sources): a PURE LLM-free
# detector + EXACT-token grounding backbone (always runs, fail-safe) + an in-context LLM verifier
# (claude-sonnet-4-6, no SDK) escalated ONLY for the deterministically-ungrounded suspects (most
# passes = zero Claude calls) that can only ADD recall. Conservative locked default (ungrounded ⇒
# redacted), CRITIQ_HALLUCINATION_GUARD_MODE runtime kill-switch. Grounded corpus = P23 account
# facts+narrative + P24 rep traits + P25 verbatim raw interactions + the account's own name/summary;
# transparent manifest, NO silent caps. Wired fail-safe into the coaching output path (coaching/
# guard.ts drops a point hinging on a redacted ref, inline-redacts next-step/summary; a guard error
# ships unguarded coaching). NO migration/API/UI (additive, zero data-loss); manifest logged (P17
# posture). verify-phase26 all-pass (incl. the false-grounding/cross-format/account-name/small-number
# locks) + Phase 21 regression + throwaway real-Claude probe (4 hallucinations redacted, 2 priorities
# dropped, every grounded ref kept). High-effort /code-review (3 angles) → 2 P0 grounding fixes applied
# in-branch (unanchored numeric substring false-grounding; account name missing from corpus). Merged
# review-for-main (PR #31). DEC-041. FLAGGED for Felix + expert coach: verifier-prompt/grounding QUALITY
# (the credibility-killer — validate before reps rely on it); detector recall blind spots (documented);
# wired only into coaching so far (adopt in pre-call/script/debrief); 8th extractJsonObject copy →
# shared-helper cleanup PR; CRITIQ_HALLUCINATION_GUARD_MODE=off|balanced is the field kill-switch.
# 🎯 AGGRESSIVE-AUTO SCOPE COMPLETE: the landing redesign + Phases 25 + 26 are all built+merged to
# review-for-main → that task (critiq-fullauto-aggressive-15-26) self-disabled. Phase 27 (Cold-Start UX)
# done interactively 2026-06-19 (PR #32, squash 8253c58). NEW RUN armed 2026-06-19: task
# critiq-fullauto-34-35 builds Phase 34 (quick-record + recordings inbox + manual assign + wire
# recording→coaching) then Phase 35 (add context + import transcripts + wire working-memory→coaching)
# → review-for-main, then self-stops. Specs in docs/PHASE_LEDGER.md (Phase 34/35 entries).
# 2026-06-19 (critiq-fullauto-34-35 firing #3): Phase 35 ADDITIVE parts 35a + 35b DONE
# -> merged review-for-main (PR #35, squash 22a6369). 35a: rep_context table (rep-private)
# + shared account_records.context column (mig 0018); src/lib/context.ts (access-gated
# get/set); routes GET/PUT /api/profile/context + /api/accounts/[id]/context; UI /profile +
# account "What you've told Critiq" card via shared ContextEditor + Profile nav link; rep +
# account context wired into the Phase 26 grounded corpus with synthetic tags (rep-context/
# account-context) so context is grounded, never redacted. 35b: src/lib/debrief/import.ts +
# POST /api/accounts/[id]/import (structures the paste via the SAME Phase 20 generator ->
# completed debrief -> fires 23/24 consolidation via after(); NO new pipeline) + /accounts/
# [id]/import page + ImportPanel. typecheck+build clean; verify-phase35 23/23; verify-phase
# 20/21/25/26 no-regression; throwaway real-PG probe (context round-trip, ACCESS BOUNDARY,
# grounding inclusion, import->structured-debrief->consolidation +1). /code-review high-effort
# + /design-critique (static; P0/P1 addressed in-branch). DEC-049..052. FLAGGED (UI_VERIFY_
# PENDING=35ab): live auth UI eyeball owed; rep-context grounding is account-agnostic; import
# structuring quality on raw transcripts; AI value NOT proven until 35c. Per DEC-049 (additive-
# first), 35c (WIRE Phase 25 working memory + the new context into pre-call/script/debrief/
# coaching via buildCachedSystem, Phase 26 guard intact) is DEFERRED to firing #4 -> then the
# critiq-fullauto-34-35 task self-disables (BOTH 34 and 35 incl. 35c merged).
# 2026-06-19 (critiq-fullauto-34-35 firing #4): Phase 35c DONE -> merged review-for-main
# (PR #36, squash 8cef470). WIRED the Phase 25 working-memory builder + the Phase 35a
# rep/account free-text context into all four AI consumers (pre-call 18/script 19/debrief
# 20/coaching 21). New buildConsumerWorkingMemory adapter (src/lib/workingmemory/
# forConsumer.ts) reuses the Phase-25 readers + pure assembler -> richer rep profile
# (learned P24->intake P18->none, identical at cold start) for the CACHED layer 2 + a
# VOLATILE memory context (rep/account context + account summary + last-3 raw interactions,
# budgeted; account summary falls back to account_records.summary). Pure renderAccountKnowledge
# (consumerPrompt.ts): memory->summary->cold-start note, byte-identical fallback so the cold
# path is unchanged. Cache split intact (rep profile cached, memory volatile). Caller owns the
# access boundary (account passed in, no re-fetch). Phase 26 guard UNTOUCHED -> everything the
# memory context injects is already in buildGroundedSources (no false redaction; probe: grounded
# name kept, 0 dropped). NO schema, NO UI. typecheck+build clean; verify-phase35c 32/32;
# verify-phase18/19/20/21/25/26 no-regression; throwaway real-PG+real-Claude probe (assembly,
# access boundary, grounding inclusion, guard keeps grounded refs, real before/after showing
# memory sharpening coaching). High-effort /code-review (7 angles) no correctness bugs;
# efficiency/cleanup fixes applied in-branch. DEC-053/054. FLAGGED (not gated): relaxed gate
# proves it runs not that coaching improved; rep context is account-agnostic (DEC-054); debrief
# sees prior interactions as background. 35c has NO UI -> no /design-critique.
# 🎯 SCOPE COMPLETE: Phase 34 (34a/b/c + 34d) AND Phase 35 (35a/b + 35c) are BOTH built+merged
# to review-for-main -> the critiq-fullauto-34-35 task SELF-DISABLES (update_scheduled_task
# enabled=false). Nothing further to build in this task's scope. review-for-main now holds
# Phases 2-21 + 23-27 + 33 + 34 + 35.
PHASE_NEXT=
# (scope complete — 34 + 35 both merged; task self-disabled. Next, when Felix resumes:
# Phase 28 consent attestation / Phase 36 AI auto-detect account from transcript (deferred
# fast-follow), + the AI-quality validation pass across 15-26 + 35c with the expert coach,
# + the pre-beta prod-DB/Blob separation. See /critiq-context.)
# Phase 33 DONE 2026-06-18 (aggressive-auto, relaxed gate + mandatory /design-critique):
# Marketing Landing Redesign — replaced the Phase 4 landing at `/` with the Felix-approved
# cinematic prototype, ported into Next.js 16 + React (one "use client" LandingPage component
# + scoped CSS module + next/font Space Grotesk/Inter), NOT a raw-HTML drop-in / not an iframe.
# All 9 sections faithful (hero+live-analysis panel, tension, 5-step loop, 3 pillars, learns-you
# +curve, bento, ICP, proof+testimonial, final CTA+footer); prototype's vanilla JS re-expressed
# in ONE useEffect (refs + data-* hooks) with full prefers-reduced-motion gating; CTAs → /login
# + /signup; metrics/testimonial code-flagged as ILLUSTRATIVE placeholders to swap pre-master.
# Branch phase-33/marketing-landing-redesign off review-for-main; merged review-for-main (PR
# #30, squash 44f9666). /design-critique (design-reviewer) → 3 P0 fixed (focus-visible
# ring, --faint AA contrast bump, <main> landmark) + P1/P2 (input semantics, aria-hidden
# decoratives, scroll-margin-top, RM hover). VERIFIED live in real Chrome across all 9 sections
# (full-page screenshots in the review packet); typecheck+build green, / prerenders static;
# responsive at 375px clean. NO device gate. DEC-040. FLAGGED for Felix: eyeball fidelity on the
# preview (rAF motion only confirmed progressing under the hidden-tab throttle); swap placeholder
# metrics+testimonial before master.
# NEXT = 26 Hallucination Guard (Phase 22 email-drafting is DEFERRED post-launch; 25 done this firing).
# Phase 26 gets SCRUTINIZED (it consumes the source-tagged facts/traits from P23+P24 + the
# working set from P25). After 26 → the MARKETING LANDING REDESIGN (final scope item).
# (stale, superseded by the 34/35 run) PHASE_NEXT=26/hallucination-guard
# Phase 24 DONE 2026-06-18 (aggressive-auto, relaxed gate): Semantic Memory — Rep
# Consolidation (Block 4, BACKEND, no UI). The REP side of the semantic tier and the
# mirror-image of Phase 23: a PRIVATE running profile of how a rep sells (strengths,
# recurring habits, growth areas), regenerated on the locked EVERY-10-DEBRIEFS cadence
# (not every debrief) from the rep's OWN completed debriefs across every account. Rep
# IDENTITY IS allowed (vs the rep-agnostic account summary); the profile is NOT written
# back to any shared field (rep-side data isolated). Table rep_summaries (mig 0016, one
# row/rep UNIQUE user_id; narrative + headline + traits jsonb {text,lens,sourceDebriefId}
# SOURCE-ATTRIBUTED + debriefCount/consolidatedThroughAt; CAS-claim/attempts-cap-3/
# stale-reclaim like account_summaries, keyed on the rep). Every-10 mechanic:
# repConsolidationThreshold(n)=floor(n/10)*10; debriefCount STORES the threshold, so it
# regenerates exactly at 10/20/30, never in-between (and the coarse threshold makes the
# count↔read race immune — Phase 23's Math.max guard not needed). src/lib/repconsolidation/
# (types/prompt/anthropic/store/runner): fetch claude-sonnet-4-6, adaptive thinking, no
# SDK, ONE cached methodology layer (rep-agnostic — no static-intake layer); observational-
# not-prescriptive contract (advice=Phase 21); parse DROPS traits citing an unknown debrief
# label + empty-result rejection; cross-account (each debrief block carries its account
# name). Two paths: /api/cron/consolidate-reps (CRON_SECRET, 8-59/10 vercel.json) =
# guaranteed; debrief route SECOND after() = best-effort, no-ops cheaply (no Claude call)
# unless the rep crosses a new multiple of 10. verify-phase24 68/68 + Phase 23 regression
# green + throwaway real-PG+real-Claude probe (below-threshold@5 → completed@10 →
# skipped@14 → re-consolidated@20; 2 grounded cross-account traits all traceable to real
# debrief ids; rep-private no write-back; idempotency; work-list; cascade). High-effort
# /code-review (3 angles) → P0/P1:0; 2 P2 comment fixes. NO UI → no /design-critique. NO
# DEVICE GATE. Merged review-for-main (PR #28, squash 432d986). DEC-038. FLAGGED for Felix
# + expert coach: rep-consolidation prompt + trait/narrative QUALITY + the observational-
# not-prescriptive boundary; calibration on real multi-account histories; no caching yet
# (methodology under the Sonnet min-prefix); debrief-only (scores not folded in); the
# profile is STORED-BUT-DORMANT until Phase 25 (working memory) surfaces it.
# Phase 23 DONE 2026-06-18 (aggressive-auto, relaxed gate): Semantic Memory — Account
# Consolidation (Block 4, BACKEND, no UI). The ACCOUNT side of the semantic tier:
# after every debrief a background job regenerates a single SHARED, rep-agnostic
# running summary for the account from its completed Phase 20 debriefs (Option B),
# with SOURCE ATTRIBUTION PER FACT (every fact carries the originating debrief id —
# the Phase 26 hallucination-guard setup). Table account_summaries (mig 0015, one
# row/account UNIQUE; narrative + headline + facts jsonb {text,lens,sourceDebriefId}
# + debriefCount/consolidatedThroughAt high-water marks; CAS-claim/attempts-cap-3/
# stale-reclaim like call_scores, keyed on account). src/lib/consolidation/
# (types/prompt/anthropic/store/runner): fetch claude-sonnet-4-6, adaptive thinking,
# no SDK, ONE cached methodology layer (rep-agnostic — no rep-profile layer); parse
# DROPS facts citing an unknown debrief label (attribution guardrail) + empty-result
# rejection; model attributes to labels D1.. → runner maps back to real ids. Reads ALL
# completed debriefs across reps (shared intelligence); count-based new-material
# detection resets the attempts budget. Write-back: narrative → account_records.summary
# (reserved since Phase 9) lights up the Phase 10 card with ZERO new UI; deletedAt-
# guarded, does NOT bump updatedAt. Two paths: /api/cron/consolidate-accounts
# (CRON_SECRET, 5-59/10 vercel.json) = guaranteed; debrief route after() = best-effort
# low-latency (maxDuration raised to 300). verify-phase23 56/56 + Phase 20/21
# regressions green + throwaway real-PG+real-Claude probe (9 attributed facts all
# traceable to real debriefs, idempotency, new-material reset, work-list, cascade).
# High-effort /code-review (3 angles) → P1s fixed (eligibility↔claim alignment;
# write-back updatedAt/deletedAt; after() budget; bounded work-list query; NULLS LAST;
# debriefCount race; comment drift). NO UI → no /design-critique. NO DEVICE GATE.
# Merged review-for-main (PR #27, squash 78e1c83). DEC-037. FLAGGED for Felix +
# expert coach: consolidation prompt + fact/narrative QUALITY; rep-identity isolation
# in the shared summary is PROMPT-enforced not code-enforced (validate on real data);
# no caching yet (methodology under the Sonnet min-prefix); debrief-only (scores not
# folded in); work-list scans debriefs (beta-acceptable, revisit at scale).
# Phase 21 DONE 2026-06-18 (aggressive-auto, relaxed gate): Coaching Output Layer —
# the interaction loop's payoff step (Signal PRD Step 06) + the FIRST surface to
# combine the Phase 20 debrief (subjective, neutral) with the Phase 16 call_score
# (objective) into ADVICE. Produces 1–3 grounded coaching priorities (focus + pillar
# lens + concrete action) + reinforce + one nextStep + summary, plus a three-pillar
# score display when a scored recording is linked. THE COACHING CONTRACT (mirror-image
# of DEC-035's reporter contract): here Critiq IS allowed to advise; grounded (every
# point traces to the report / open questions / score findings, never invented — sets
# up Phase 26), conservative (1–3 priorities, no padding), prioritizes toward the
# weakest scored pillar when a score exists, else coaches from the debrief alone;
# empty coaching (no priorities AND no nextStep) rejected. Lens taxonomy SHARED with
# the debrief. DEBRIEF-SCOPED (mirrors script-on-brief): /accounts/[id]/debrief/
# [debriefId]/coaching; call_coaching table (migration 0014, additive, dev Neon,
# hand-written SQL — drizzle journal stale at 0007). One row/generation, latest
# completed shown; recordingId/scoreId SET NULL + scoreSnapshot jsonb (objective score
# snapshotted at gen time so advice + display stay coherent after a re-score).
# src/lib/coaching/ (types/prompt/anthropic/store/generate) mirrors Phase 20: fetch
# claude-sonnet-4-6, adaptive thinking, no SDK, defensive parse + lens coercion +
# empty-coaching rejection; synchronous (no cron/CAS); reuses Phase 17 buildCachedSystem
# + Phase 18 buildRepProfileBlock. Routes POST/GET/PATCH (owner-gated; account+debrief
# match; PATCH 1–5 rating). UI CoachingPanel below the rendered debrief (ScoreCard when
# linked, numbered priorities w/ lens pills, reinforce, dark "Your next move", rating,
# re-coach; remounts by debrief id so a fresh debrief starts un-coached).
# /design-critique P0:0, P1 fixed (shared buttonClass, slate-400→500/600 contrast).
# High-effort /code-review P0/P1:0 (ownership boundaries hold; migration↔schema parity;
# coaching-reset-on-new-debrief correct). verify-phase21 65/65 + Phase 20 regression
# green; throwaway real-PG + real-Claude probe (access boundary, debrief-not-ready,
# score linkage + snapshot, latest=completed, ownership, rating, cascade cleanup + two
# live round-trips — grounded, actionable, scored path led with implication = lowest
# SPIN sub-score). NO DEVICE GATE. Merged review-for-main (PR #26, squash 6e6d467).
# DEC-036. FLAGGED for Felix + expert coach: coaching prompt + priority/next-step
# structure + grounding/conservatism quality; field calibration; the three-pillar SCORE
# PATH IS DOCUMENTED-DORMANT (debrief UI never sets recordingId → beta = debrief-only;
# built + unit-tested + probe-verified, goes live when recording→debrief linkage lands);
# per-phase shared-UI duplication (rating widget / extractJsonObject / Spinner) worth a
# dedicated cleanup PR; live auth UI walk needs a reachable preview.
# Phase 20 DONE 2026-06-18 (aggressive-auto, relaxed gate): Post-Call Debrief —
# Reporter Mode (Block 3, the interaction loop, UI+AI) — Signal PRD Step 05. The
# rep REPORTS what happened via guided observational prompts (text; voice=Phase 2);
# Critiq STRUCTURES it into recap + neutral observations (each tagged a SPIN/Voss/
# Navarro lens) + commitments + openQuestions + summary. THE REPORTER CONTRACT: it
# organizes, it does NOT grade/score/advise (scoring=call_scores/Phase 16; coaching
# ADVICE=Phase 21) — neutrality is unit-asserted in the system prompt; this lets
# Phase 21 layer coaching + Phase 23 consolidate without double-counting judgment.
# Account-scoped (DEC-035 conflict-res: no `calls` entity → /accounts/[id]/debrief,
# mirrors Phase 18/19; the "call" = the debrief + an optional recording). call_debriefs
# table (migration 0013, additive, dev Neon); nullable recordingId/briefId SET NULL
# loop-links (Phase 21/23 joins, not wired in UI — beta reps debrief un-recorded
# calls); raw `report` stored verbatim (episodic source of truth). src/lib/debrief/
# (types/reporter/prompt/anthropic/store/generate) mirrors Phase 18: fetch
# claude-sonnet-4-6, adaptive thinking, no SDK, defensive parse + lens coercion +
# empty-debrief rejection; reporter.ts = single source for prompts→UI fields+guards+
# narration; reuses Phase 17 buildCachedSystem + Phase 18 buildRepProfileBlock;
# synchronous (no cron/CAS). Routes POST/GET /api/accounts/[id]/debrief + PATCH
# .../[debriefId] (owner+account match; 1-5 rating). UI /accounts/[id]/debrief +
# DebriefPanel (guided form → structured debrief, per-lens pills, rating, localStorage
# draft) + account-card CTA. /design-critique P0:0, P1 fixed (h1 truncate, aria-busy,
# per-lens pill tint, de-duped intro). High-effort /code-review P0:0 (no correctness
# bugs — faithful, stricter mirror of Phase 18) + 2 hardenings (setDebriefRating
# scopes UPDATE to userId; client coerces jsonb arrays like the server page).
# verify-phase20 64/64 + Phase 18 regression green; throwaway real-PG+real-Claude
# probe 17/17 (neutral, grounded, zero grading/advice). NO DEVICE GATE. Merged
# review-for-main (PR #25, squash c5d80be). DEC-035. FLAGGED for Felix + expert coach:
# reporter prompt + guided-prompt set + lens taxonomy quality; field calibration;
# voice=Phase 2; decide whether to switch Phase 18's objective-handoff count source
# to completed debriefs (the true call count this phase adds); live auth UI walk needs
# a reachable preview.
# Phase 19 DONE 2026-06-18 (aggressive-auto, relaxed gate): Script Generation with
# Delivery Cues + Style Modes (Block 3, the interaction loop, UI+AI) — PRD Step 04.
# Turn a COMPLETED Phase 18 brief into a delivery-cued conversation framework in a
# chosen style mode. TWO style modes (Assertive/Relational, PRD §06; baseline seam
# resolveBaselineStyleMode -> default Relational, overridable per call, persists via
# localStorage). Inline cues per line: emphasis|pace|pause|register|silence (PRD §04).
# Email draft (PRD Step 04 "if needed") = Phase 22, EXCLUDED. call_scripts table
# (migration 0012, additive, dev Neon); one row/generation; objective SNAPSHOTTED at
# gen time. src/lib/script/ (style/prompt/anthropic/store/generate) mirrors Phase
# 16/18: fetch claude-sonnet-4-6, adaptive thinking, no SDK, defensive parse + cue/
# section coercion + empty-script rejection; styleMode from the rule not the model
# echo; synchronous (no cron/CAS). Reuses Phase 17 buildCachedSystem + Phase 18
# buildRepProfileBlock. Routes POST/GET/PATCH /api/accounts/[id]/pre-call/[briefId]/
# script (owner-gated; PATCH=1-5 rating). UI CallScriptPanel below the brief
# (RadioCardGroup chooser, Write/Rewrite, opener/sections+cue pills/closing/delivery
# notes, mode+objective header, rating). /design-critique P1 fixed (reuse
# RadioCardGroup). /code-review high-effort P1 fixed (getLatestScriptForBrief filters
# completed so a failed rewrite can't bury the last good script) + P2s (drop wasted
# rep-profile query; PATCH validates account segment; surface script mode+objective).
# verify-phase19 65/65; throwaway real-PG+real-Claude probes 18/18 (both modes, valid
# cues, snapshot, guards). NO DEVICE GATE. Merged review-for-main (PR #24, squash
# 658f148). DEC-034. FLAGGED for Felix + expert coach: script prompt/cue structure +
# style-mode definitions quality; field calibration; baseline is a default-only seam
# until intake carries a style signal; caching needs a real rep-profile prefix; live
# authenticated UI walk needs a reachable preview.
# Phase 18 DONE 2026-06-18 (aggressive-auto, relaxed gate): Pre-Call Brief Flow —
# the interaction loop's prep step (PRD Step 03). Rep narrates account context +
# call goal (text; voice = Phase 2) -> Critiq returns a diagnostic brief
# (diagnosis + approach + objections + quick-read summary) + the call objective.
# OBJECTIVE HANDOFF = HARD RULE (resolveObjectiveMode, unit-tested): rep sets on
# interactions 1-2, Critiq recommends on 3+, rep can override (deviation recorded);
# mode is server-derived, a signal brief with no recommendation FAILS CLOSED.
# Script generation w/ delivery cues is Phase 19 (kept out). pre_call_briefs table
# (migration 0011, additive, dev Neon). src/lib/precall (objective/prompt/anthropic/
# repProfile/store/generate) — FIRST live consumer of the two-layer prompt cache
# (methodology forever + rep intake per rep) via Phase 17's buildCachedSystem.
# Routes POST/GET /api/accounts/[id]/pre-call + PATCH .../[briefId] (override +
# 1-5 rating). UI /accounts/[id]/pre-call + PreCallBriefPanel + account-card CTA;
# /accounts added to PROTECTED_PREFIXES. /design-critique P0:0 + 4 P1 fixed (44px
# touch targets, slate accent, badge parity, submit spinner). /code-review P0:0,
# P1s+P2s fixed. verify-phase18 58/58; throwaway real-PG+real-Claude probes 17/17
# + 6/6 (incl. an evidence-grounded sample brief). NO DEVICE GATE. Merged
# review-for-main (PR #23, squash 50506f0). DEC-033. FLAGGED for Felix + expert
# coach: prep prompt + brief structure quality; field-context calibration;
# interaction-count is a proxy until Phase 20+; voice = Phase 2; live auth UI walk
# + <3-min timing need a reachable preview.
# Phase 17 DONE 2026-06-18 (aggressive-auto, relaxed gate): Anthropic prompt
# caching wiring + cache telemetry. Reusable layer src/lib/ai/cache.ts —
# buildCachedSystem places cache_control breakpoints across ordered most-stable-
# first layers (methodology forever + rep intake per rep, the locked memory arch;
# each its own breakpoint; drops empty layers; caps at 4); summarizeCacheUsage/
# formatCacheUsage read the usage object NaN-safely; MIN_CACHEABLE_TOKENS advisory.
# cache_control is GA (no beta header). Scoring builds its system via the helper
# (replaces Phase 16's inline breakpoint); AnthropicScorer gained an onUsage hook;
# the runner LOGS the per-call cache hit rate (counts only, no PII). Telemetry by
# logging, NOT a schema migration (additive, zero data-loss). NO UI (no /design-
# critique), NO migration. verify-phase17 36/36 + verify-phase16 regression green;
# typecheck+build clean. LIVE PROBE (throwaway): methodology block 1567 tok caches
# on claude-sonnet-4-6 -> call1 created=1560, call2 read=1560 = 90% hit rate;
# caching is live on the scoring call TODAY. DEC-032. FLAGGED for Felix: the
# claude-api doc lists a 2048 min for Sonnet 4.6 but the live API cached at ~1560
# (set MIN_CACHEABLE_TOKENS sonnet=1024 to match observation, advisory-only);
# telemetry is log-only (no dashboard yet); rep-intake cache layer is tested but
# has no live consumer until coaching/working-memory (18-21, 25). Merged
# review-for-main (PR #22, squash abb912a).
# Phase 25 DONE 2026-06-18 (aggressive-auto, relaxed gate): Working Memory Assembly — the
# WORKING tier of the locked memory architecture (Episodic→Semantic→WORKING). A PURE builder
# (src/lib/workingmemory/) that assembles, under a per-call TOKEN BUDGET, methodology(rubric) +
# rep profile + this account's profile + last 3 raw interactions into a working set for a Claude
# call. NO table/migration/API route/UI — reads existing tiers only (additive, zero data-loss).
# Cache-aware split: stableLayers (methodology cached-forever + rep profile cached-per-rep, typed
# as the reused CacheableLayer → buildCachedSystem) + volatileContext (account summary + recent
# raw → user message, never cached) + a transparent manifest (every include/trim/truncate + token
# math, NO silent caps). Budget default 6000; deterministic trim policy (foundation methodology+rep
# NEVER trimmed; account summary outranks the raw tail; raw filled newest-first, truncate-then-drop).
# estimateTokens = ~4-chars/tok heuristic (rounds up; API usage is ground truth). Rep-profile
# cold-start fallback: learned summary (P24) → static intake (P18) → none. RAW INTERACTIONS SCOPED
# REP-PRIVATE (userId+accountId, DEC-039) — shared account intel flows via the rep-agnostic P23
# summary; malformed empty-`happened` rows filtered IN SQL before LIMIT. build.ts buildWorkingMemory
# (userId, accountId) gated by getAccountForUser (null for an unassigned rep). PRIMITIVE BUILT +
# TESTED but NOT yet wired into the existing AI consumers (pre-call/script/debrief/coaching) — a
# flagged follow-up (Phase 17 cache-helper posture). verify-phase25 62/62 + throwaway real-PG probe
# (access boundary, learned→intake fallback, account summary, rep-private scope, newest-first,
# older-than-cap disclosure, empty-row exclusion, cascade). High-effort /code-review (3 angles) →
# fixes applied in-branch (raw-header token undercount → count actual emitted text + reserve header;
# maxRawInteractions honored at the assembler; exact drop accounting; truncation back-half boundary;
# CacheableLayer reuse; resolveAccountSummary→string|null; memoized METHODOLOGY_BLOCK; empty-row
# guard moved to SQL). NO UI → no /design-critique. Merged review-for-main (PR #29, squash c2496bf).
# DEC-039. FLAGGED for Felix + expert coach: the 6000 budget + trim PRIORITY + last-3 cap; methodology
# block ~929 tok sits under the Sonnet 1024 cacheable floor on its own (caches once per-rep layer
# extends the prefix); confirm the rep-private raw scope is intended; the ~4-chars/tok estimate is a
# heuristic not a tokenizer; WIRING the consumers onto the builder is the real payoff + is NOT done.
PHASE_LAST_COMPLETED=38/recording-analysis-page (core; 38c/38d audio still open)
PHASE_LAST_COMPLETED_AT=2026-06-21T00:00:00-04:00
# Phase 38 CORE merged to review-for-main 2026-06-21 (PR #38, squash 4ac5416): 38a assign-flow
# inline-create fix, 38b /recordings/[id] page + honest states, 38e scorecard render, 38f scoring
# -> Opus 4.8 (caching confirmed LIVE on Opus, no prompt growth), the VALUE-PATH Blob-read 403 fix
# (transcription now reads private chunks via @vercel/blob get(access:'private')), + a readable/
# copyable transcript view. Felix verified record -> transcript -> Opus assessment END-TO-END live
# on the preview ("worked well"). review-for-main now holds Phases 2-21 + 23-27 + 33 + 34 + 35 + 37
# + 38(core); master still 2-13. NEXT = 38c (audio player) + 38d (synced transcript) on a NEW branch
# off review-for-main, gated on a real >=10-min recording (iPhone for the MP4 path) + the playback/
# seek device gate; audio-contract design half locked in docs/CALL_ANALYSIS_AUDIO_CONTRACT.md. Then 39-44.
# Phase 37 (account & data controls + workspace nav) merged to review-for-main (PR #37, squash 6ab6d9b),
# verified live (P0 hard-delete end-to-end). NEXT (pending Felix's go) = the CALL-ANALYSIS experience:
# robust approach delivered via the `robust-call-analysis-approach` workflow (research+design+architecture+
# plan) — /recordings/[id] detail (playback + synced transcript + full assessment + evidence) + a call-pill
# Analysis button + fix the assign-flow bug + close the score<->memory loop. 7 phases, 5 full-auto-able;
# Felix owns the audio device-gate + the AI-feedback calibration (6-7). Model rec: Opus 4.8 for scoring+coaching.

# --- ENVIRONMENT READINESS (checked by /critiq-full-auto Step 1d before any DB phase) ---
# DATABASE_URL provisioned (dedicated Critiq Neon project, PG 18.4) + in Vercel prod/dev.
# Also in .env.local: ANTHROPIC_API_KEY + RESEND_API_KEY (reused), AUTH_SECRET + CRON_SECRET.
# DEEPGRAM_API_KEY PROVISIONED 2026-06-18 (Felix's key; validated vs Deepgram API;
#   Vercel prod + preview/all-branches Sensitive + .env.local) -> Phase 15 unblocked.
# Still to provision: SENTRY_DSN (Phase 4/29).
# Phase 11 + 11a done + device-verified + promoted to review-for-main; client-side only, no env.
# BLOB_READ_WRITE_TOKEN PROVISIONED 2026-06-06 (Felix, dashboard) — store critiq2-blob (store_LgqOObB7jYpcT6x8),
#   scoped Preview/all-branches + Sensitive. Sensitive => NOT pullable to .env.local; verify Phase 12 on the PREVIEW.
#   Pre-launch blob TODOs (decided "later" with Felix 2026-06-06): (1) add Production scope to the token before prod
#   launch; (2) stand up a SEPARATE preview blob store (or strict path prefix) so beta test recordings don't mix with
#   real prod audio (same lesson as the preview/prod env-sharing note). Not blocking Phase 12 (which runs on previews).
ENV_READY=true
# UI_VERIFY_PENDING: a headless cron sets this when it ships a UI phase it could
# not drive via MCP; the next interactive session drives it via Chrome MCP, then
# clears it. Empty = nothing pending.
# CLEARED 2026-06-06 (interactive /critiq-full-auto):
#   - Phases 2-8 verified earlier; Phases 9 & 10 driven via Chrome MCP 2026-06-05.
#   - Phase 11/11a: the ON-DEVICE GATE PASSED on Felix's iPhone 2026-06-06 — the
#     neutral "Critiq" lock-screen widget is PRESENT (the L3 branding the Phase 11
#     oscillator never surfaced; fixed by Phase 11a's looping <audio> rewrite), and
#     the keep-alive held green across app switches. Desktop drive (Chrome MCP, real
#     preview) also confirmed the <audio> element + the CSP fix (errorCode 4 -> null,
#     media-src blob:); sustained playback isn't observable in the hidden MCP tab
#     (Chrome media throttle, same artifact as Wake Lock "Off") -> the device gate
#     is the real proof.
#   Recording stack (11 + 11a) promoted recording-staging -> review-for-main (PR #13).
#   CLEARED 2026-06-07: Phase 12 DEVICE-GATE PASSED on iPhone (capture, keep-alive,
#   offline-recover-on-reconnect) -> merged to recording-staging (PR #14, c7cb620).
# 2026-06-08 (Phase 14 cron build): interruption detection + multi-channel
#   notification (chime + tab-flash + Web Push + in-app banner) BUILT to OPEN
#   PR #18 (base recording-staging). Headless cron = baseline only (build/types/
#   unit, 32 assertions). Awaits Felix's iPhone DEVICE GATE: chime + tab-flash +
#   Web Push + banner on a real mic-grab, branding-only on the lock screen, then
#   recover-clears-all. VAPID keypair generated -> add to Vercel for runtime push
#   (PR #18 body has the keys). Do NOT merge until the gate passes; then merge to
#   recording-staging + promote to review-for-main per Felix.
# CLEARED 2026-06-18: Phase 14 device gate PASSED (notifications on iPhone; coverage
# session b47ea693 ~91%, gap_ms=10005 persisted). VAPID + Deepgram keys provisioned.
# Phase 14 merged (PR #18, c517ce7) + promoted to review-for-main.
# 2026-06-19: 35ab added to the pending list — the /profile rep-context page, the account
# "What you've told Critiq" card, and /accounts/[id]/import need a live authenticated UI
# eyeball on the preview (this firing was headless; SSO-walled preview; no browser).
UI_VERIFY_PENDING=
# CLEARED 2026-06-21: 35ab was driven live earlier this session, and the Phase 38 CORE + the
# record->transcript->Opus-assessment value path were verified LIVE by Felix on the preview
# ("worked well"). The only remaining device gate is 38c/38d AUDIO (a real >=10-min iPhone
# recording for playback/seek) — tracked in the ledger, not a headless UI_VERIFY debt.

# --- SESSION HEARTBEAT ---
# Phase 21 DONE 2026-06-18 (aggressive-auto run, scheduled task critiq-fullauto-
# aggressive-15-26). Coaching Output Layer (Block 3, the interaction loop, UI+AI)
# merged to review-for-main (PR #26, squash 6e6d467) -> review-for-main now holds
# Phases 2-21. First surface combining the Phase 20 debrief (subjective) + Phase 16
# call_score (objective) into ADVICE. /design-critique pass run (P0:0, P1 fixed) per
# the UI-phase rule; high-effort /code-review run (P0/P1:0). Relaxed gate
# (build+types+unit) applied; no device gates remain. NEXT = Phase 23 (Semantic
# Memory: Account Consolidation — Block 4, a BACKEND phase: background regeneration of
# the shared account running summary after every debrief, with source attribution per
# fact). Phase 22 (email drafting) is DEFERRED post-launch (aggressive-auto scope skips
# it). Block 4 (23-25) is the cleanest unattended-cron window in the build; Phase 26
# (hallucination guard) gets scrutinized. Phase 23 has NO UI → no /design-critique pass
# needed; it consumes the call_debriefs episodic records this phase + Phase 20 produce.
# Phase 23 DONE 2026-06-18 (aggressive-auto, scheduled task critiq-fullauto-
# aggressive-15-26). Account Consolidation (Block 4, backend) merged to review-for-main
# (PR #27) → review-for-main now holds Phases 2-21 + 23. Built directly by the
# orchestrator (DEC-004/013/030.. precedent for a fully-specified backend phase), the
# cleanest unattended-cron window in the build. Relaxed gate (build+types+unit) + a
# real-PG+real-Claude probe; high-effort /code-review P1s fixed. NO UI → no
# /design-critique; NO device gate. NEXT = Phase 24 (Semantic Memory: Rep Consolidation
# — runs every 10 debriefs, regenerates the per-rep running summary; mirrors Phase 23's
# consolidation engine but rep-scoped + rep-private, and IS allowed rep identity). Then
# 25 (working memory) + 26 (hallucination guard — gets scrutinized; it CONSUMES Phase
# 23's source-tagged facts).
# Phase 24 DONE 2026-06-18 (aggressive-auto run, scheduled task critiq-fullauto-
# aggressive-15-26). Rep Consolidation (Block 4, backend) merged to review-for-main
# (PR #28, squash 432d986) → review-for-main now holds Phases 2-21 + 23 + 24. Mirror of
# Phase 23 rep-scoped + rep-private + every-10-debriefs cadence. Built directly by the
# orchestrator (fully-specified backend phase, DEC-037/004/013/030.. precedent), the
# cleanest unattended-cron window. Relaxed gate (build+types+unit) + a real-PG+real-Claude
# probe; high-effort /code-review (3 angles) P0/P1:0, 2 P2 comment fixes. NO UI → no
# /design-critique; NO device gate. NEXT = Phase 25 (Working Memory Assembly — the THIRD
# semantic-memory tier: a prompt builder that loads rep profile [Phase 24] + account
# summary [Phase 23] + last 3 raw interactions + the rubric into the working set, with a
# per-call token budget). Then 26 (hallucination guard — gets scrutinized; it CONSUMES the
# source-tagged facts/traits from Phase 23 + 24). The rep-profile QUALITY is FLAGGED for
# Felix + the expert coach (not blocking the relaxed gate).
# Phase 25 DONE 2026-06-18 (aggressive-auto run, scheduled task critiq-fullauto-aggressive-15-26).
# Working Memory Assembly (Block 4, the WORKING tier, BACKEND, no UI) merged to review-for-main
# (PR #29, squash c2496bf) → review-for-main now holds Phases 2-21 + 23 + 24 + 25. Built directly
# by the orchestrator (fully-specified backend phase, DEC-039/038/037/004/013 precedent). Relaxed
# gate (build+types+unit 62/62) + a throwaway real-PG probe; high-effort /code-review (3 angles)
# P0/P1:0, all fixes applied in-branch. NO UI → no /design-critique; NO device gate. NEXT = Phase 26
# (Hallucination Guard — gets SCRUTINIZED; it CONSUMES the source-tagged facts/traits from P23+P24
# and the working set from P25; conservative-mode defaults locked). After 26 → the MARKETING LANDING
# REDESIGN (final scope item, build ONLY after 15-26 all merged). Working-memory QUALITY (budget/
# trim/scope) is FLAGGED for Felix + the expert coach; the builder is built-but-not-yet-wired into
# the AI consumers (the real payoff, a flagged follow-up).
# Phase 33 DONE 2026-06-18 (aggressive-auto, scheduled task critiq-fullauto-aggressive-15-26).
# Marketing Landing Redesign merged to review-for-main (PR #30) → review-for-main now holds
# Phases 2-21 + 23 + 24 + 25 + 33. The IMMEDIATE-NEXT scope item (built before resuming the
# numbered order; Phase 25 was already done). Cinematic prototype ported into real Next.js/React
# (CSS module + next/font), /design-critique P0/P1 addressed, verified live across all 9 sections.
# NEXT = Phase 26 (Hallucination Guard — gets SCRUTINIZED; consumes the source-tagged facts/traits
# from P23+P24 and the P25 working set). After 26 → scope complete (landing + 25 + 26 all done) →
# self-disable this task.
# Phase 26 DONE 2026-06-18 (aggressive-auto run, scheduled task critiq-fullauto-aggressive-15-26).
# Hallucination Guard (the conservative pre-output validator, the credibility guardrail) merged to
# review-for-main (PR #31) → review-for-main now holds Phases 2-21 + 23 + 24 + 25 + 26 + 33. The
# FINAL scope item. Built as a primitive (src/lib/hallucinationguard/) consuming the P23/P24
# source-tagged facts/traits + the P25 raw interactions as the grounded corpus, wired fail-safe into
# the coaching output path. Relaxed gate (build+types+unit) + a throwaway real-Claude probe; high-
# effort /code-review (3 angles) → 2 P0 grounding fixes applied in-branch. NO migration/API/UI;
# manifest logged. DEC-041. AI-quality (verifier prompt + grounding judgment) FLAGGED for Felix + the
# expert coach with sample outputs in the review packet — NOT gated. 🎯 SCOPE COMPLETE: the marketing
# landing redesign + Phase 25 + Phase 26 are ALL built+merged → the scheduled task self-disables
# (update_scheduled_task enabled=false). Nothing further to build in this task's scope.
SESSION_LAST_HEARTBEAT=2026-06-21T00:00:00-04:00
SESSION_TOKEN_BUDGET_USED=0
SESSION_ID=interactive-phase38-core-2026-06-21

# --- CRON SCHEDULE ---
# Phase 8 merged live/supervised 2026-06-05. ARMED: hourly self-bounding LOCAL
# scheduled task "critiq-fullauto-9-10-11" (cron "0 * * * *", fires ~:06 past each
# hour, local TZ) -> builds Phase 9 & 10 into review-for-main and Phase 11 into
# recording-staging, then self-stops (next phase > 11 -> exit + best-effort
# disable via update_scheduled_task). Phase 11 is browser-media a headless cron
# cannot verify -> it sets UI_VERIFY_PENDING=11 and stops for joint device
# verification. Overlap guard: a firing skips if PHASE_CURRENT is set with a
# heartbeat < 90 min old. Only fires while the desktop app is open (closed ->
# runs on next launch). First run ~2026-06-05T18:06 ET / 22:06 UTC (Phase 9).
# SCOPE BOUND REACHED 2026-06-05 ~20:20 ET: the hourly cron built its terminal
# phase (11 -> recording-staging) and self-stops. Next un-built phase is 12,
# which is OUTSIDE this task's 9-11 scope -> the task disables itself (best-effort
# update_scheduled_task enabled=false). Phase 12+ is Felix-gated (Blob token +
# joint device verification of Phase 11). Do NOT re-arm without Felix's go.
NEXT_CRON_FIRE_ET=disabled-scope-bound-reached
CRON_SCHEDULE_LABEL=critiq-fullauto-9-10-11-COMPLETE-self-disabling

# --- BLOCKERS ---
# 2026-06-08: Phase 12 PROMOTED recording-staging -> review-for-main (PR #15, merge
# 1503a6f) by Felix's explicit command. review-for-main now holds Phases 2-12.
# 🚦 MASTER MERGE (PR #1 review-for-main -> master) PAUSED — Felix explicitly commanded it
# this session, but verification found PRODUCTION IS NOT PROVISIONED, so merging now would
# ship a broken prod (master is still the bare scaffold; the only production-target deploy is
# a redeploy of the scaffold commit 0bc1b60 -> promoting = the FIRST real prod deploy). Per
# the hard rule (friction in the verification path -> STOP, do NOT degrade to merge-anyway),
# the merge was left for Felix (DEC-026). Production is MISSING (Preview-only): AUTH_SECRET
# (NextAuth REQUIRES it -> auth dead), RESEND_API_KEY, AUTH_RESEND_KEY, EMAIL_FROM,
# BLOB_READ_WRITE_TOKEN. DATABASE_URL/_UNPOOLED have SEPARATE Production entries (distinct
# from Preview) -> likely a different Neon branch that is UNMIGRATED (0001-0004 applied to the
# dev Neon only). [RESOLVED 2026-06-08] PROD ENV FULLY PROVISIONED + PROD DB SCHEMA PRESENT:
# (1) ✅ Claude added a FRESH prod AUTH_SECRET. (2) ✅ Claude added prod RESEND_API_KEY + AUTH_RESEND_KEY
# + EMAIL_FROM + ANTHROPIC_API_KEY + CRON_SECRET (reused preview values). (3) ✅ Felix added
# BLOB_READ_WRITE_TOKEN to Production. (4) ✅ Migration vs the prod DB ran (Claude, via
# `vercel env pull --environment=production` + scripts/migrate.ts): ALL 0000-0005 already applied ->
# prod schema READY. ⚠️ FINDING: prod DATABASE_URL points to the SAME Neon DB as dev/preview (endpoint
# ep-restless-field-apvp3l79; prod = the direct endpoint, dev = the -pooler endpoint of the same DB) ->
# ALL environments share ONE database (incl. the existing p8verify/rs-test test data + Phase 12-13 test
# recordings). Fine PRE-LAUNCH (no real data), but BEFORE real beta users provision a SEPARATE prod Neon
# (else preview testing reads/writes prod data — the ApexTrust lesson, see memory feedback_apex_branch_preview_db).
# NOT a merge blocker. ✅ MASTER MERGED 2026-06-09 (PR #1, merge 3c2e198) by Felix's explicit command:
# Phases 2-13 SHIPPED to master = first prod deploy; PROD VERIFIED HEALTHY at critiq2.vercel.app (200s on
# landing/login/signup; /dashboard 307 = auth runs; zero prod runtime errors; DB = the shared migrated DB).
# Prod fully provisioned. BEFORE REAL BETA REPS: separate prod Neon DB + separate prod blob store (prod
# currently shares ONE DB/store with dev+preview; see memory project_critiq_prelaunch_checks). DEC-029.
BLOCKER_REASON=
BLOCKER_SET_AT=2026-06-08T03:21:00-04:00

# --- EMAIL CHANNEL ---
EMAIL_SENDER=notifications@critiq.firstlap.dev
EMAIL_RECIPIENT=felix@firstlap.dev
EMAIL_PR_COMMENT_FALLBACK=true
