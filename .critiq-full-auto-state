# Critiq Full Auto — Runtime State
# Edit STOP=true from any device to halt at the next cron firing.
# Edit URL: https://github.com/FelixNava/Critiq2/edit/review-for-main/.critiq-full-auto-state
# This file is read by /critiq-full-auto skill at session start.
# Format: KEY=VALUE, one per line, lines starting with # are comments.

# --- KILL SWITCH (the only field you should ever edit manually) ---
STOP=false

# --- BUILD METADATA ---
BUILD_START_ET=
BUILD_LABEL=critiq-beta-foundation-test
BUILD_TOTAL_TOKEN_BUDGET=4000000
BUILD_PHASE_TOKEN_BUDGET=800000

# --- PHASE TRACKING ---
PHASE_CURRENT=
PHASE_CURRENT_STATUS=
PHASE_CURRENT_PR=
PHASE_NEXT=2/drizzle-neon
PHASE_LAST_COMPLETED=1/foundation-rails
PHASE_LAST_COMPLETED_AT=2026-06-05T02:10:00-04:00

# --- ENVIRONMENT READINESS (checked by /critiq-full-auto Step 1d before any DB phase) ---
# Secrets pre-staged in .env.local: ANTHROPIC_API_KEY + RESEND_API_KEY (reused from
# sibling projects, account-wide), AUTH_SECRET + CRON_SECRET (freshly generated).
# MISSING: DATABASE_URL — must be a dedicated Critiq Neon DB (never a sibling's).
ENV_READY=false

# --- SESSION HEARTBEAT ---
SESSION_LAST_HEARTBEAT=
SESSION_TOKEN_BUDGET_USED=0
SESSION_ID=

# --- CRON SCHEDULE ---
# UNARMED. Path B chosen 2026-06-05 ~02:00 ET: build the rails, do NOT arm the cron.
# Arming is a deliberate next step AFTER DATABASE_URL is provisioned and (recommended)
# a manual dry-run of Phase 2 passes. See /critiq-full-auto + the AM summary.
NEXT_CRON_FIRE_ET=
CRON_SCHEDULE_LABEL=UNARMED-path-b-rails-only

# --- BLOCKERS ---
# Precondition for Phase 2+. Auto-clears at runtime once DATABASE_URL is set
# (the env-readiness gate re-checks live; this string is advisory for /critiq-status).
BLOCKER_REASON=Phase 2 needs DATABASE_URL. Felix: create a dedicated Critiq Neon project (console.neon.tech), paste the POOLED connection string into .env.local + Vercel env. Do NOT reuse a sibling DB. Everything else is pre-staged.
BLOCKER_SET_AT=2026-06-05T02:10:00-04:00

# --- EMAIL CHANNEL ---
EMAIL_SENDER=notifications@critiq.firstlap.dev
EMAIL_RECIPIENT=felix@firstlap.dev
EMAIL_PR_COMMENT_FALLBACK=true
